[behave]
# Tell Behave where to look for feature files
paths = features

# Don't hide print/log output during test run — easier to debug
stdout_capture = false
log_capture = false
stderr_capture = false

# Show each step result clearly in terminal
verbose = true